# チャンバラ道場

ブラウザで遊べるシンプルな一騎打ちチャンバラゲームです。

## 遊び方

- `上段斬り / 中段突き / 下段払い` で攻撃。
- `上段受け / 中段受け / 下段受け` で防御の構えを変更。
- 相手の構えを読むと大ダメージ、読まれると小ダメージ。
- 先に相手の体力を 0 にしたら勝ち。

## 起動

静的ファイルだけなので、次のどちらかで起動できます。

```bash
python3 -m http.server 4173
```

その後 `http://localhost:4173` を開いてください。
